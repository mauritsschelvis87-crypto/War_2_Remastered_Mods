<Window x:Class="SetupApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quality of Life Modding"
        Width="560" Height="390" MinWidth="520" MinHeight="350"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBrush}" Foreground="White"
        ResizeMode="CanMinimize">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="22" FontWeight="SemiBold" Text="Quality of Life Modding"/>
        <TextBlock Grid.Row="1" Margin="0,7,0,0" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"
               Text="Install the Warcraft II Remastered color and quality-of-life tools."/>

        <StackPanel Grid.Row="2" Margin="0,22,0,0">
            <TextBlock Text="Warcraft II Remastered" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" Height="34" VerticalContentAlignment="Center" Padding="8,0"
                         ToolTip="The folder containing the x86\Data folder"/>
                <Button Grid.Column="1" Content="Browse" Width="92" Height="34" Margin="10,0,0,0"
                        Click="Browse_Click"/>
            </Grid>
            <CheckBox x:Name="DesktopShortcutCheckBox" IsChecked="True" Margin="0,14,0,0"
                      Foreground="{StaticResource TextBrush}"
                      Content="Create a desktop shortcut"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="0,16,0,0" TextWrapping="Wrap"
                   Foreground="{StaticResource MutedBrush}" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="InstallButton" Content="Install and open" Width="150" Height="38"
                    Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}"
                    Click="Install_Click"/>
                <Button Content="Cancel" Width="92" Height="38" Margin="10,0,0,0"
                    IsCancel="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
