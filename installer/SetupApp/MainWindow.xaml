<Window x:Class="SetupApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quality of Life Mods — Setup"
        Width="560" Height="340" MinWidth="520" MinHeight="300"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBrush}" Foreground="White"
        ResizeMode="CanMinimize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Warcraft II Remastered — Quality of Life Mods"/>
        <TextBlock Grid.Row="1" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"
                   Text="Installs the modding app into your Warcraft II Remastered folder."/>

        <StackPanel Grid.Row="2" Margin="0,22,0,0">
            <TextBlock Text="Game install folder" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" Height="34" VerticalContentAlignment="Center" Padding="8,0"
                         Background="{StaticResource CardBrush}" Foreground="White"
                         BorderBrush="#FF4A6388"/>
                <Button Grid.Column="1" Content="Browse..." Width="100" Height="34" Margin="10,0,0,0"
                        Click="Browse_Click"/>
            </Grid>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="0,16,0,0" TextWrapping="Wrap"
                   Foreground="{StaticResource MutedBrush}" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="InstallButton" Content="Install" Width="120" Height="36"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderBrush="#FF4CC37A" Click="Install_Click"/>
            <Button Content="Close" Width="100" Height="36" Margin="10,0,0,0"
                    IsCancel="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
